[project]
name = "invoice"
version = "0.3.0"
description = "Finetuned LayoutLMv3 model for invoice number extraction"
authors = [{ name = "Ryan Z. Nie", email = "ryanznie@gatech.edu" }]
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["invoice", "ner", "layoutlmv3", "document-ai", "ocr"]

dependencies = [
    "pandas>=2.3.2",
    "pillow>=11.3.0",
    "tqdm>=4.67.1",
    "torch",
    "transformers",
    "numpy>=1.24.0",
    "google-genai==1.57.0", 
    # Web app dependencies
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "gradio>=4.0.0",
    "python-dotenv>=1.0.0",
    # ONNX and deployment dependencies
    "onnx>=1.15.0",
    "onnxruntime>=1.16.0",
    "onnxruntime-tools>=1.7.0",
    "onnxconverter-common>=1.14.0",
    "tritonclient[http]>=2.41.0",
    "prometheus-client>=0.19.0",
]

[project.optional-dependencies]
dev = [
    "dvc>=3.63.0",
    "ipykernel>=6.30.1",
    "prek",
    "streamlit>=1.49.1",
    "datasets",
    "peft",
    "accelerate",
    "evaluate",
    "scikit-learn",
    "seqeval>=1.2.2",
    # Testing dependencies
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    # Benchmarking dependencies
    "wandb>=0.16.0",
    "pyyaml>=6.0.0",
    "google-genai",
]

[project.scripts]
invoice-ner-app = "app:main"
invoice-ner-preprocess = "scripts.preprocess:main"
invoice-ner-benchmark = "benchmarks.benchmark:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
only-include = [
    "app.py",
    "scripts/*.py",
    "tests/*.py",
]

[tool.uv]
required-environments = ["sys_platform == 'darwin'"]

[tool.coverage.run]
relative_files = true
branch = true
source = ["."]

[dependency-groups]
dev = [
    "prek>=0.3.3",
]
